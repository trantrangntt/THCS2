#include <bits/stdc++.h>
using namespace std;
int snt(int n)
{
    for (int i = 2; i <= sqrt(n); i++)
    {
        if (n % i == 0)
            return i;
    }
    return n;
}
int tach(int n)
{
    int m;
    while (n > 0)
    {
        m = n % 10;
        +-
    }
}
int main()
{
    int t;
    cin >> t;
    while (t--)
    {
        int n, a;
        cin >> n >> a;
    }
}