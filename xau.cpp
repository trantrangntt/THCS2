khai báo sâu string a;
nhập nếu cin thì k lưu dấu cách
        giờ getline(cin, a)
            cout
    << s;
+ hàm lấy độ dài:s.length()
                     nếu 1 kí tự dùng char bình thường

                 ten_vector.ten_ham();
Tách sâu stringstream
    string token;
string a[100];
khai báo(stringsteam ten(a))
    hàm while (ten >> token)
{
    cout << token << endl;
}
trống chôi lệnh cin.ignore()
    hàm xóa kí tự s.erase(i, n) với xóa từ vị trí i và xóa n kí tự;